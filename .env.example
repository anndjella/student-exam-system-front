# Leave empty during local development to use the Vite /api proxy.
# For a separately hosted frontend, set this to the deployed backend origin.
# New infrastructure (infra/ in the backend repo) API: https://app-studentexam-api-swc.azurewebsites.net
VITE_API_BASE_URL=
